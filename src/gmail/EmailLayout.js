import React from "react";
import { StyleSheet } from "react-native";
import { Avatar } from "react-native-elements";
import { Box, Text } from "../components/theme";

const EmailLayout = ({ emailItem }) => {
  return null;
};

export default EmailLayout;
